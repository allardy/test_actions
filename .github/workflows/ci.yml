name: CI
on:
  workflow_call:
    inputs:
      branchOrCommit:
        description: 'Branch/Commit/Tag'
        required: false
        type: string
        default: 'master'
      skip:
        description: 'Skip'
        required: false
        type: boolean
        default: false

jobs:
  format:
    if: github.event_name != 'workflow_call' || !inputs.skip
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

  lint:
    if: github.event_name != 'workflow_call' || !inputs.skip
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI"
      - run: sleep 10
